{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "client.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "browserify client.js -o browser/client-bundled.js && cp game.html browser/game.html && cp clientcss.css browser/clientcss-bundled.css"
    },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dgram": "^1.0.1",
    "socket.io": "^2.3.0",
    "socket.io-client": "^2.2.0"
  }
}
